package restAssuredAPI;

import org.testng.annotations.Test;

public class UpdateResource extends BaseTest {
    @Test
    public void updateUser () {
        //        final JSONObject requestData = new JSONObject ();
        //        requestData.put ("name", "abc");
        //        requestData.put ("job", "test");
        //        final Response response = given ().contentType (ContentType.JSON)
        //            .
        //                body (requestData)
        //            .
        //                when ()
        //            .
        //                put ("/api/users/2");
        //        System.out.println (response.getBody ()
        //            .prettyPeek ());
        //        Assert.assertEquals (response.getStatusCode (), 200,
        //            "Status code mismatch expected 200 ,Received : " + response.getStatusCode ());

    }
}
